import { EstiloHermanosDirective } from './estilo-hermanos.directive';

describe('EstiloHermanosDirective', () => {
  it('should create an instance', () => {
    const directive = new EstiloHermanosDirective();
    expect(directive).toBeTruthy();
  });
});
