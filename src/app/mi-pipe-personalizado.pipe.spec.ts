import { MiPipePersonalizadoPipe } from './mi-pipe-personalizado.pipe';

describe('MiPipePersonalizadoPipe', () => {
  it('create an instance', () => {
    const pipe = new MiPipePersonalizadoPipe();
    expect(pipe).toBeTruthy();
  });
});
